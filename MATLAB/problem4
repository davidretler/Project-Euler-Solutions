%%Project Euler Problem Number Four
%%Find the largest palindrome made from the product of two 3-digit numbers.
%%David Etler, 30 June 2012

function out = checkpalindrome(num)                                                 %Checks if the imputed number is palindromic 
    out = (num2str(num,1000) == (num2str(num,1000))(length(str):-1:1))              %Reverse the string and check equality
    return
end

function out = main()
    products = [];                                                                  %Initialize empty products array
    for i = 100:999                                                                 %For all three-digit numbers, twice
        for j = 100:999
            products(length(products)+1) = i.*j                                     %Append the array with the product
        end
    end
    out = max(products(checkpalindrome(products)));
    return
end
